sudo: required

services:
  - docker

language: go

go:
- 1.8.3

deploy:
  provider: script
  script:
    - make
    - make docker
    - make push
  on:
    all_branches: true

deploy:
  provider: script
  script:
    - make
    - make docker
    - make push
    - make crossbuild
    - make release
  on:
    tags: true